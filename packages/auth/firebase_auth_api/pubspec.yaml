name: firebase_auth_api
description: Implementation of AuthApi using Firebase 
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.1
  auth_api:
    path: ../auth_api

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  mocktail: ^1.0.4
  plugin_platform_interface: ^2.1.8

